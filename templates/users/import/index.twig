{% extends "layouts/admin.twig" %}

{% block title %}UM | Import Files{% endblock %}

{% block content %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Import Files</h1>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="row">
                <form class="form form-horizontal" enctype="multipart/form-data" role="form" method="post" action="/um/files/import">
                    {% if error %}
                        <div class="alert alert-danger" role="alert">
                            <strong>Oh snap!</strong> {{ error }}
                        </div>
                    {% endif %}
                    <div class="col-lg-12">
                        <label for="file">Upload Current Files</label>
                        <input type="file" name="file" id="file">
                        <p class="help-block">The batch file contains the information of credit cards in csv format.</p>
                        <button type="submit" class="btn btn-default">Submit</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="row">
                <form class="form form-horizontal" enctype="multipart/form-data" role="form" method="post" action="/um/files/import/beginning">
                    {% if errorBeginning %}
                        <div class="alert alert-danger" role="alert">
                            <strong>Oh snap!</strong> {{ errorBeginning }}
                        </div>
                    {% endif %}
                    <div class="col-lg-12">
                        <label for="file">Upload Beginning Files</label>
                        <input type="file" name="file" id="file">
                        <p class="help-block">The batch file contains the information of credit cards in csv format.</p>
                        <button type="submit" class="btn btn-default">Submit</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}